services:
  tasks-api-db:
    container_name: tasks_api_database
    image: postgres:16
    restart: always
    env_file:
      - .env
    volumes:
      - ./postgres:/var/lib/postgresql/data
    ports:
      - 5432:5432